<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.healthmapper.chwapp">

    <!-- Permissions for CHW Disease Reporting -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="CHW Disease Reporter"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Main Activity - Entry Point -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile Activity - CHW Profile Management -->
        <activity
            android:name=".ProfileActivity"
            android:label="CHW Profile"
            android:exported="false" />

        <!-- History Activity - Disease Reports History -->
        <activity
            android:name=".HistoryActivity"
            android:label="Reports History"
            android:exported="false" />

    </application>

</manifest>